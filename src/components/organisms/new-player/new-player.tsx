import Modal from '../../atoms/modal/modal'

const NewPlayer = () => {
  return (
    <Modal closeHandler={() => {}} display={true} centerModalMobile={true}>
      <h1>data</h1>
    </Modal>
  )
}

export default NewPlayer
